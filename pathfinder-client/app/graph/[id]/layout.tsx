"use client"

import { ReactNode } from 'react'

// Main layout component that combines provider and initialization
export default function GraphLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
} 